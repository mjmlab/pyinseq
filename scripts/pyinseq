#!/usr/bin/env python3
from pyinseq import runner
import sys

if __name__ == '__main__':
    args = sys.argv[1:]
    if args[0] == 'demultiplex':
        print('whew!')
    else:
        runner.main(args)
